function fact( Int n ):Int
{
    if( n == 0 )
        return 1;
    else
        return n * fact( n - 1 );
}

function firstNSum( Int n ):Int
{
    if( n == 0 )
        return 0;
    else
        return n + firstNSum( n - 1 );
}

print( firstNSum(10) );
